/*
 * Stub for the standalone CoverageHost test target. Provides just enough of the real
 * KNShelfSplitView class for AIXtrasManager.m to compile without an AdiumY.framework binary.
 * The TU only uses it as a parameter type.
 */
#import <Cocoa/Cocoa.h>

@interface KNShelfSplitView : NSView
@end
